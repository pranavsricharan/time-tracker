const COLOR_PRIMARY = '#2185d0';

const DEFAULT_THEME = {
  palette: {
      type: 'dark',
      primary: {
          main: COLOR_PRIMARY,
      },
      
  }
};

const LIGHT_THEME = {
  palette: {
      primary: {
          main: COLOR_PRIMARY,
      },
      
  }
};

export { DEFAULT_THEME, LIGHT_THEME }